<div class="list_item account <%= account.type.to_s.underscore%> <%= account.positive? ? 'positive_account' : 'negative_account' %>">
	<div class="field first name"><%=h account.name %></div>
	<div class="field description"><%=h account.description %></div>
	<div class="field balance"><%=h number_to_currency(account.balance) %></div>
	<div class="field type"><%=h account.type.to_s.titleize %></div>
	<div class="field user"><%=h account.user.username %></div>
	<div class="field due_date"><%=h account.due_date.short_date %></div>
	<div class="field period"><%=h Account::Period[account.period].name %></div>
	<div class="links">
		<div class="link show"><%= link_to 'Show', account %></div>
		<div class="link edit"><%= link_to 'Edit', edit_account_path(account) %></div>
		<div class="link destroy"><%= link_to 'Destroy', account, :confirm => 'Are you sure?', :method => :delete %></div>
	</div>
</div>
