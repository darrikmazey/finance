<%
	sc = account_item.class.all_superclasses
	@output = nil
	while @output.nil?
		c = sc.shift
		@output = render :partial => c.underscore, :object => account_item rescue nil
	end
%>

<%= @output %>

<%= render :partial => 'account_item', :collection => account_item.children %>
