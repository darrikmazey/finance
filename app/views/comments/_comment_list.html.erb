<div class="list comments">
	<div class="heading">comments <%= @work_item.nil? ? link_to('+', new_comment_url) : link_to('+', new_work_item_comment_url(@work_item)) %></div>
	<div class="content">
		<%= render :partial => 'comments/list_header' %>
		<%= render :partial => 'comments/comment', :collection => comment_list %>
	</div>
</div>
